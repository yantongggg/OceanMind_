fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2
networkx>=3.2
httpx>=0.27
sse-starlette>=2.1

# Live signal ingestion (backend/ingest/)
feedparser>=6.0          # RSS/Atom parsing for maritime feeds + news search
python-dotenv>=1.0       # loads backend/.env
supabase>=2.9            # signal persistence; optional — degrades to in-memory
